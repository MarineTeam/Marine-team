# ============================================================
# Server-side env vars for the Vercel serverless function (api/embed.js).
# Set these in Vercel → Project → Settings → Environment Variables.
# NEVER commit real values, and never put the bunny token key in js/config.js.
# ============================================================

# Auth0 tenant domain (used to verify the member's access token via /userinfo)
AUTH0_DOMAIN=your-tenant.us.auth0.com

# bunny.net Stream library that holds your sermon videos
BUNNY_LIBRARY_ID=123456

# bunny.net library → Security tab → Token Authentication key (SECRET)
BUNNY_TOKEN_AUTH_KEY=your-bunny-token-authentication-key

# bunny.net library-level API key (SECRET) — used by /api/videos to list the
# library so staff can sync + publish videos from the admin dashboard.
BUNNY_API_KEY=your-bunny-library-api-key

# Optional: library CDN hostname for thumbnails (e.g. vz-xxxx.b-cdn.net)
BUNNY_CDN_HOSTNAME=

# Supabase — /api/videos verifies the staff member's Supabase token before
# returning the library. Same URL + anon key as js/config.js (anon is public).
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
